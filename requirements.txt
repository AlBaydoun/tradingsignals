# Core numerical stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3

# Data acquisition
requests>=2.31
feedparser>=6.0
PyYAML>=6.0
python-dateutil>=2.8

# API server (optional — only needed for `signalforge-api`)
fastapi>=0.110
uvicorn[standard]>=0.27

# Claude reasoning layer (optional — the engine runs without it)
anthropic>=0.40
